## Guide for Uploading Code to Github 

* ```git init```

* ```git add . / <filename with extension>```

* ```git commit -m "<committed message>"```

* ```git remote add <name> <remote>```

* ```git branch -M main```

* ``` git push -u origin main```
